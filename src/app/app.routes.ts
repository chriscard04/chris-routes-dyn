import { Routes } from '@angular/router';
import { HomeRoutesComponent } from './home-routes/home-routes.component';

export const routes: Routes = [
  { path: '', component: HomeRoutesComponent }
];
